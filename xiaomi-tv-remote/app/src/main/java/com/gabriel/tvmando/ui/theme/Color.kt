package com.gabriel.tvmando.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * Paleta pensada para usarse a oscuras, delante de la tele.
 *
 * Fondos casi negros para que el movil no deslumbre, un unico acento calido
 * (nada de azules a las once de la noche) y tres colores de estado que se leen
 * de un vistazo sin tener que enfocar la vista.
 */

val Ink = Color(0xFF08090C)
val InkRaised = Color(0xFF11131A)
val InkHigh = Color(0xFF1A1D26)
val Hairline = Color(0xFF272B36)

val Chalk = Color(0xFFF2F3F5)
val ChalkMuted = Color(0xFF858C9B)
val ChalkFaint = Color(0xFF5A606D)

/** Acento de la app: naranja de brasa, calido y visible sin ser fluorescente. */
val Ember = Color(0xFFFF5A1F)
val EmberSunk = Color(0xFF2E1409)

/** Estados de conexion. */
val Signal = Color(0xFF35D07F)
val Waiting = Color(0xFFFFB020)
val Alert = Color(0xFFFF4D5E)
